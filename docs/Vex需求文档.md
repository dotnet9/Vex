# Vex Markdown编辑器 需求整理文档
## 一、基础信息
1. **产品名称**：Vex
2. **中文名称**：维刻
3. **Slogan**：极简之力，妙笔成章
4. **作者**：沙漠尽头的狼
5. **网站**：CodeWF（码坊），网址https://codewf.com

## 二、核心定位与技术参考
1. **产品类型**：Markdown编辑器
2. **功能对标**：仿Typora功能
3. **技术参考**：基础技术对齐示例`E:\github\libs\CodeWF.Markdown\src\CodeWF.Markdown.Sample`，用户体验参考Typora
4. **开发规范**：标准Avalonia开发规范
5. **参考素材**：截图路径`C:\Users\liu64\Pictures\1`

## 三、界面布局要求
1. 菜单放置于标题栏，不占用客户区
2. 客户区分左、中、右三栏布局：
   - 左侧：文件|大纲 TabControl
   - 中间：Markdown编辑器
   - 右侧：Markdown预览
3. 底部：状态栏，展示状态数据

## 四、菜单功能要求
### 1. 文件菜单
新建、新建窗口、打开、打开文件夹、快速打开、打开最近文件、选择编码重新打开、保存、另存为、保存全部打开的文件、属性、打开文件位置、删除、导出、打印、关闭

### 2. 编辑/段落/格式菜单
功能与Typora保持一致

### 3. 视图菜单
显示/隐藏侧边栏、大纲、文档列表、搜索、源代码模式、显示状态栏、字数统计窗口、切换全屏、保持窗口在最前端、实际大小、放大、缩小

### 4. 主题菜单
分为**主题色**、**排版**两个大菜单项，迁移CodeWF.Markdown.Sample相关功能

### 5. 国际化菜单
迁移CodeWF.Markdown.Sample相关功能

### 6. 帮助菜单
更新日志、快速开始（引导）、鸣谢、官方网站、反馈、关于

## 五、技术框架与依赖
### 1. 第三方UI库（可学习使用）
- Semi.Avalonia：路径`E:\github\third_libs\Semi.Avalonia`
- Ursa.Avalonia：路径`E:\github\third_libs\Ursa.Avalonia`

### 2. 自研库使用
- 仓库路径：`E:\github\libs`
- 使用方式：通过NuGet包安装，文档和代码不标注自研；通用修改可直接调整，本地打包使用，后续验证后同步发布至NuGet

### 3. 架构与通信
- 模块化管理：Prism 8.X版本
- 通信组件：CodeWF.EventBus（模块、View、ViewModel间通信）

### 4. 跨平台发布要求
1. **Windows**：支持AOT发布、支持Win7；需安装VC-LTL和YY-Thunks最新NuGet包（预览版亦可）
2. **Linux/macOS**：支持单文件自包含发布

## 六、项目架构与开发规范
1. **代码规范**：符合开源项目规范，模块分离彻底，使用优质设计模式，禁止代码耦合
2. **自定义控件**：创建`Vex.Controls`和`Vex.Controls.Themes`，按Semi.Avalonia风格组织，功能模块彻底分离
3. **日志要求**
   - 开发日志：完成一个小功能即记录
   - 更新日志：记录功能迭代（增/删/优化），提供中英双语版本，区别于开发日志（精简不啰嗦）

   日志规范格式：

   ```markdown
   # 更新日志

   ## 12.0.3.13（2026-05-19）

   - ✨[新增]-脑图中心主题支持左键拖拽平移整张画布，和已有触控板、滚轮、`Space + 左键`、中键平移保持一致。
   - 🔧[优化]-加大脑图默认左侧、右侧和上方不可见画布空间，让拖拽中心主题平移时不容易过早撞到边界，并恢复脑图水平/竖直滚动条的按需显示。
   - 🔧[优化]-打开、导入或应用内置模板后，脑图视图会自动把中心主题放到左侧中线附近，减少加载后手动找根节点的步骤。
   - 🧪[测试]-构建 `CodeWF.MindView`、`CodeWF.MindView.Themes` 和 `Zhijian -f net10.0`，确认中心主题拖拽平移相关交互改动编译通过。
   - ✨[新增]-在脑图面板为空白文档增加快速开始操作条，可直接添加子节点、打开使用手册、导入文件或切换到 Markdown。
   - 🔧[优化]-文件页空状态改用 AtomUI `Empty` 与图标按钮，恢复有意义的系统窗口标题，并让状态栏工具按钮在最小窗口尺寸下保持稳定。
   - 🧪[测试]-构建 `src/Zhijian/Zhijian.csproj -f net10.0-windows`，启动桌面程序，并截图验证空白文档起步条、文件空状态、系统标题和最小尺寸状态栏。
   - 🔧[优化]-新增 `CodeWF.Tools.Core`，About 窗口改为通过 `AssemblyExtensions` 读取 `Version` 和 `CompileTime`，移除硬编码版本元数据。
   - 🔧[优化]-将 About 窗口标签和说明文案迁入语言资源，让新增的编译时间标签也遵循应用本地化模型。
   ```
4. **提交规范**：英文规范化提交代码并推送

## 七、第三方组件引用规范
1. 必须查询开源协议，优先选择MIT/Apache-2.0/BSD协议组件
2. 禁止引用不明组件、黑盒组件
3. 需穿透检查间接依赖，确保所有依赖均符合源码开放标准
